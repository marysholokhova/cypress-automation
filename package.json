{
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --config video=false --reporter mochawesome --browser chrome"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^1.8.2",
    "cypress": "^5.2.0",
    "cypress-log-to-output": "^1.1.2",
    "mochawesome": "^6.1.1"
  },
  "dependencies": {
    "cypress-commands": "^1.1.0"
  }
}
